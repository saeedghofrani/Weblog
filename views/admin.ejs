<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./common/header.ejs', {Document: "admin" }) %>
        <link rel="stylesheet" href="/style/dashboard.css">
        <link rel="stylesheet" href="/style/admin.css">
</head>

<body>
    <%- include('./common/navbar.ejs') %>
        <section class="pricing-section">
            <div class="container">
                <div id="appendDiv" class="row">
                    <% for( let i=0; i < users.length; i++ ) { %>
                        <div class="col-12 col-sm-6 col-md-5 col-lg-4 my-2 <%= users[i]._id %>">
                            <div class="price-card featured<% if (users[i].role === 'admin') { %>
                                    admin
                                <% } %>">
                                <h2>
                                    <%= users[i].role %>
                                </h2>
                                <p class="text-success ">
                                    <span class="text-muted">Username:</span>  <%= users[i].username %>
                                </p>
                                <ul class="pricing-offers ">
                                    <li class="text-success">
                                        <span class="text-muted">First Name:</span>  <%= users[i].firstName %>
                                    </li>
                                    <li class="text-success ">
                                        <span class="text-muted">Last Name:</span>  <%= users[i].lastName %>
                                    </li>
                                    <li class="text-success ">
                                        <span class="text-muted">Email:</span>  <%= users[i].email %>
                                    </li>
                                    <li class="text-success ">
                                        <span class="text-muted">Phone:</span>  <%= users[i].phone %>
                                    </li>
                                    <li class="text-success ">
                                        <span class="text-muted">Gender:</span>  <%= users[i].gender %>
                                    </li>
                                    <li class="text-success ">
                                        <span class="text-muted">Status:</span>  <%= users[i].status %>
                                    </li>
                                    <li class="col-12 ">
                                        <button class="col-12 button-37 resetPass" userId="<%= users[i]._id %>">Reset Password</button>
                                    </li>
                                    <li class="col-12 ">
                                        <button class="col-12 button-37 deleteUser" userId="<%= users[i]._id %>">Remove User</button>
                                    </li>
                                </ul>
                            </div>
                        </div>
                        <% } %>
                </div>
            </div>
        </section>


        <div class="toast" style="position: absolute; top: 20px; right: 20px;">
            <div class="toast-header">
                <strong class="mr-auto">Message</strong>
                <button id="toastBtn" type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="toast-body">

            </div>
        </div>
        
        <%- include('./common/scripts.ejs') %>
            <script src="/script/admin.js"></script>
</body>

</html>